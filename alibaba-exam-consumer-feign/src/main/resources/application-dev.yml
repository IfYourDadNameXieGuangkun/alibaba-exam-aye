spring:
  application:
    name: nacos-consumer-feign
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: consumer-feign
server:
  port: 9991

name: "dev"
management:
  endpoints:
    web:
      exposure:
        include: "*"
